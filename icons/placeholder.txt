<!doctype html>
<html lang="zh-Hant">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <meta name="theme-color" content="#101827" />
  <meta name="description" content="CLEAR — Structured Clarity System. Get clear. Then move." />
  <link rel="manifest" href="./manifest.webmanifest" />
  <title>CLEAR</title>
  <style>
    :root{
      --bg1:#0b1020;
      --bg2:#1a2450;
      --bg3:#2a2f5c;
      --fg:#eef2ff;
      --muted:rgba(238,242,255,.75);
      --card:rgba(255,255,255,.06);
      --border:rgba(255,255,255,.12);
      --btn:rgba(255,255,255,.10);
      --btnHover:rgba(255,255,255,.14);
      --shadow: 0 20px 60px rgba(0,0,0,.35);
      --radius: 18px;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      color:var(--fg);
      font-family: -apple-system, BlinkMacSystemFont, "SF Pro Display", "SF Pro Text",
                   "PingFang TC","Noto Sans TC","Microsoft JhengHei", system-ui, sans-serif;
      background:
        radial-gradient(1200px 800px at 20% 10%, rgba(114,190,255,.18), transparent 60%),
        radial-gradient(1000px 700px at 80% 30%, rgba(183,120,255,.16), transparent 55%),
        radial-gradient(900px 700px at 50% 90%, rgba(120,255,210,.10), transparent 55%),
        linear-gradient(160deg, var(--bg1), var(--bg2) 55%, var(--bg3));
      overflow-x:hidden;
    }
    .wrap{
      min-height:100%;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:24px 16px;
    }
    .card{
      width:min(720px, 100%);
      background:var(--card);
      border:1px solid var(--border);
      border-radius:var(--radius);
      box-shadow: var(--shadow);
      backdrop-filter: blur(10px);
      padding:24px;
    }
    .brand{
      display:flex;
      align-items:baseline;
      justify-content:space-between;
      gap:12px;
      margin-bottom:14px;
    }
    .brand h1{
      margin:0;
      letter-spacing:.12em;
      font-weight:700;
      font-size:20px;
    }
    .brand .tag{
      font-size:12px;
      color:var(--muted);
      letter-spacing:.08em;
      text-transform:uppercase;
      white-space:nowrap;
    }
    .headline{
      margin:10px 0 6px;
      font-size:28px;
      line-height:1.2;
      letter-spacing:-.02em;
      font-weight:650;
    }
    .sub{
      margin:0 0 18px;
      color:var(--muted);
      line-height:1.6;
      font-size:15px;
    }
    .divider{
      height:1px;
      background:rgba(255,255,255,.10);
      margin:18px 0;
    }
    .btnrow{display:flex;flex-wrap:wrap;gap:10px;margin-top:14px}
    button{
      appearance:none;
      border:1px solid var(--border);
      background:var(--btn);
      color:var(--fg);
      padding:12px 14px;
      border-radius:14px;
      cursor:pointer;
      font-size:15px;
      line-height:1.1;
      transition:.15s ease;
    }
    button:hover{background:var(--btnHover)}
    button.primary{
      background:rgba(238,242,255,.18);
      border-color:rgba(238,242,255,.22);
    }
    button.primary:hover{
      background:rgba(238,242,255,.22);
    }
    .small{
      font-size:12px;
      color:var(--muted);
      line-height:1.6;
      margin-top:10px;
    }
    .step{
      display:flex;
      align-items:center;
      gap:10px;
      margin:0 0 10px;
      color:var(--muted);
      font-size:13px;
      letter-spacing:.04em;
    }
    .pill{
      padding:6px 10px;
      border:1px solid var(--border);
      border-radius:999px;
      background:rgba(255,255,255,.06);
      color:var(--fg);
      font-weight:600;
      font-size:12px;
      letter-spacing:.08em;
    }
    .q{
      font-size:20px;
      line-height:1.35;
      margin:8px 0 6px;
      font-weight:650;
      letter-spacing:-.01em;
    }
    .hint{margin:0;color:var(--muted);line-height:1.6}
    .input{
      margin-top:12px;
      width:100%;
      border:1px solid var(--border);
      background:rgba(0,0,0,.18);
      color:var(--fg);
      border-radius:14px;
      padding:12px 12px;
      font-size:15px;
      outline:none;
    }
    .footer{
      margin-top:16px;
      display:flex;
      flex-wrap:wrap;
      gap:10px;
      align-items:center;
      justify-content:space-between;
      color:var(--muted);
      font-size:12px;
    }
    a{color:inherit}
  </style>
</head>
<body>
  <div class="wrap">
    <main class="card" id="app" aria-live="polite"></main>
  </div>

  <script src="./app.js"></script>
  <script>
    if ('serviceWorker' in navigator) {
      window.addEventListener('load', () => {
        navigator.serviceWorker.register('./sw.js').catch(()=>{});
      });
    }
  </script>
</body>
</html>
